# Спутник

Современное веб-приложение на Next.js с настроенным CI/CD pipeline, Docker и автоматическим деплоем на Vercel.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

### Сборка для production

```bash
npm run build
npm start
```

## 🐳 Docker

### Запуск с Docker Compose (Production)

```bash
docker-compose up -d
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

### Запуск в режиме разработки с Docker

```bash
docker-compose -f docker-compose.dev.yml up
```

### Сборка Docker образа вручную

```bash
docker build -t sputnik:latest .
```

### Запуск Docker контейнера

```bash
docker run -p 3000:3000 sputnik:latest
```

### Остановка контейнеров

```bash
docker-compose down
```

## 📋 Доступные команды

- `npm run dev` - Запуск dev сервера
- `npm run build` - Сборка приложения для production
- `npm start` - Запуск production сервера
- `npm run lint` - Проверка кода с помощью ESLint
- `npm run lint:fix` - Автоисправление ошибок ESLint
- `npm run type-check` - Проверка типов TypeScript
- `npm run format` - Форматирование кода с помощью Prettier
- `npm run format:check` - Проверка форматирования кода
- `npm run validate` - Запуск всех проверок (типы, линтинг, форматирование, сборка)

## 🌿 Структура веток

- `main` - Production ветка, автоматический деплой
- `dev` - Development ветка, автоматический деплой в dev окружение
- `feature/**` - Ветки для новых функций
- `bugfix/**` - Ветки для исправления багов
- `hotfix/**` - Ветки для срочных исправлений

## 🔄 CI/CD Pipeline

### CI (Continuous Integration)

При каждом push в любую ветку и при создании Pull Request автоматически запускается:

1. **Install Dependencies** - Установка зависимостей
2. **Code Quality Checks** - Проверка форматирования, ESLint и типов TypeScript
3. **Build Application** - Сборка приложения для проверки отсутствия ошибок
4. **Build and Push Docker Image** - Сборка Docker образа и пуш в GitHub Container Registry (GHCR)
   - Образ доступен по адресу: `ghcr.io/<owner>/<repo>:<tag>`
   - Теги: `dev`, `main`, `latest`, SHA коммита

### CD (Continuous Deployment)

- **Dev окружение**: Автоматический деплой при push в ветку `dev`
- **Production окружение**: Автоматический деплой при push в ветку `main`

### Docker образы в GHCR

После успешной сборки Docker образы автоматически публикуются в GitHub Container Registry:

- **Dev ветка**: `ghcr.io/<owner>/<repo>:dev`
- **Main ветка**: `ghcr.io/<owner>/<repo>:main` и `ghcr.io/<owner>/<repo>:latest`
- **Другие ветки**: `ghcr.io/<owner>/<repo>:<branch-name>`

Для использования образа:

```bash
docker pull ghcr.io/<owner>/<repo>:latest
docker run -p 3000:3000 ghcr.io/<owner>/<repo>:latest
```

## 📝 Workflow разработки

1. Создайте feature ветку от `dev`:

   ```bash
   git checkout dev
   git pull origin dev
   git checkout -b feature/your-feature-name
   ```

2. Внесите изменения и закоммитьте:

   ```bash
   git add .
   git commit -m "feat: описание изменений"
   ```

   Pre-commit хук автоматически проверит:
   - Форматирование кода (Prettier)
   - Линтинг (ESLint)
   - Типы TypeScript
   - Сборку приложения

3. Запушьте ветку и создайте Pull Request в `dev`:

   ```bash
   git push origin feature/your-feature-name
   ```

4. После ревью и мерджа в `dev`, изменения автоматически деплоятся в dev окружение

5. Для деплоя в production создайте Pull Request из `dev` в `main`

## 🛠 Технологии

- **Next.js 14** - React фреймворк
- **TypeScript** - Типизация
- **ESLint** - Линтинг кода
- **Prettier** - Форматирование кода
- **Docker** - Контейнеризация
- **GitHub Actions** - CI/CD
- **Vercel** - Хостинг и деплой

## 📦 Структура проекта

```
.
├── app/                      # Next.js App Router
│   ├── layout.tsx           # Корневой layout
│   ├── page.tsx             # Главная страница
│   └── globals.css          # Глобальные стили
├── .github/
│   └── workflows/           # GitHub Actions workflows
│       ├── ci.yml           # CI pipeline (проверки + Docker сборка)
│       ├── deploy.yml       # Production деплой
│       ├── dev-deploy.yml   # Dev деплой
│       └── pr-check.yml     # PR проверки
├── .husky/                  # Git hooks
│   └── pre-commit           # Pre-commit проверки
├── Dockerfile               # Docker образ для production
├── docker-compose.yml       # Docker Compose для production
├── docker-compose.dev.yml   # Docker Compose для разработки
├── .dockerignore            # Исключения для Docker
├── vercel.json              # Конфигурация Vercel
├── next.config.js           # Конфигурация Next.js
├── tsconfig.json            # Конфигурация TypeScript
├── .eslintrc.json           # Конфигурация ESLint
├── .prettierrc              # Конфигурация Prettier
└── package.json             # Зависимости проекта
```

## 🔧 Настройка деплоя

### Настройка Vercel

1. **Получите Vercel Token:**
   - Зайдите на https://vercel.com/account/tokens
   - Создайте новый токен

2. **Добавьте секрет в GitHub:**
   - Settings → Secrets and variables → Actions
   - New repository secret
   - Name: `VERCEL_TOKEN`
   - Value: ваш токен

3. **Настройка Vercel проекта:**
   - Подключите репозиторий к Vercel
   - Vercel автоматически обнаружит Next.js проект
   - В настройках проекта: **Settings** → **General** → **Build & Development Settings**
   - Убедитесь, что **Framework Preset** установлен на **"Next.js"**
   - Vercel автоматически использует оптимальные настройки для Next.js:
     - Автоматическая оптимизация изображений
     - Edge Functions для лучшей производительности
     - Автоматическое кэширование
     - Оптимизация CSS и шрифтов
     - Компрессия ответов

   **Оптимизации включены:**
   - ✅ SWC минификация
   - ✅ Оптимизация шрифтов
   - ✅ Оптимизация изображений (AVIF, WebP)
   - ✅ Компрессия ответов
   - ✅ Безопасные заголовки (X-Content-Type-Options, X-Frame-Options, X-XSS-Protection)
   - ✅ Удаление console.log в production
   - ✅ Автоматическое кэширование статических ресурсов

   **Примечание:** Docker образы в GHCR доступны для других платформ (Kubernetes, AWS ECS, локальный запуск и т.д.), но Vercel использует нативную сборку Next.js для максимальной производительности

### Настройка GitHub Container Registry

Docker образы автоматически публикуются в GHCR при каждом push. Для доступа к образам:

1. Перейдите в Packages в вашем репозитории GitHub
2. Образы будут доступны по адресу: `ghcr.io/<owner>/<repo>`

### Использование Docker образа локально

```bash
# Pull образа из GHCR
docker pull ghcr.io/<owner>/<repo>:latest

# Запуск контейнера
docker run -p 3000:3000 ghcr.io/<owner>/<repo>:latest
```

### Переменные окружения

Для настройки переменных окружения в Vercel:

1. Перейдите в настройки проекта в Vercel
2. Settings → Environment Variables
3. Добавьте необходимые переменные для каждого окружения (Production, Preview, Development)

## 🔒 Pre-commit проверки

При каждом коммите автоматически запускаются:

1. **Lint-staged** - Автоисправление измененных файлов
2. **TypeScript type check** - Проверка типов
3. **ESLint** - Линтинг кода
4. **Prettier** - Проверка форматирования
5. **Build** - Проверка сборки

Если какая-либо проверка не проходит, коммит будет заблокирован.

## 📚 Дополнительные ресурсы

- [Next.js Documentation](https://nextjs.org/docs)
- [TypeScript Documentation](https://www.typescriptlang.org/docs)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Docker Documentation](https://docs.docker.com/)
- [Vercel Documentation](https://vercel.com/docs)

## 🐛 Решение проблем

### Docker образ не собирается

- Убедитесь, что `next.config.js` содержит `output: 'standalone'`
- Проверьте, что все зависимости указаны в `package.json`

### Vercel деплой не работает

- Проверьте, что `VERCEL_TOKEN` добавлен в секреты GitHub
- Убедитесь, что проект подключен к Vercel
- Проверьте логи в GitHub Actions

### Pre-commit проверки блокируют коммит

- Запустите `npm run lint:fix` для автоисправления
- Запустите `npm run format` для форматирования
- Проверьте типы: `npm run type-check`
