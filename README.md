# Max Hack

Next.js приложение с настроенным CI/CD pipeline.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

### Сборка для production

```bash
npm run build
npm start
```

## 📋 Доступные команды

- `npm run dev` - Запуск dev сервера
- `npm run build` - Сборка приложения для production
- `npm start` - Запуск production сервера
- `npm run lint` - Проверка кода с помощью ESLint
- `npm run type-check` - Проверка типов TypeScript
- `npm run format` - Форматирование кода с помощью Prettier
- `npm run format:check` - Проверка форматирования кода

## 🌿 Структура веток

- `main` - Production ветка, автоматический деплой
- `dev` - Development ветка, автоматический деплой в dev окружение
- `feature/**` - Ветки для новых функций
- `bugfix/**` - Ветки для исправления багов
- `hotfix/**` - Ветки для срочных исправлений

## 🔄 CI/CD Pipeline

### CI (Continuous Integration)

При каждом push в любую ветку и при создании Pull Request автоматически запускается:

1. **Lint & Type Check** - Проверка кода ESLint и типов TypeScript
2. **Format Check** - Проверка форматирования кода
3. **Build** - Сборка приложения для проверки отсутствия ошибок

### CD (Continuous Deployment)

- **Dev окружение**: Автоматический деплой при push в ветку `dev`
- **Production окружение**: Автоматический деплой при push в ветку `main`

## 📝 Workflow разработки

1. Создайте feature ветку от `dev`:

   ```bash
   git checkout dev
   git pull origin dev
   git checkout -b feature/your-feature-name
   ```

2. Внесите изменения и закоммитьте:

   ```bash
   git add .
   git commit -m "feat: описание изменений"
   ```

3. Запушьте ветку и создайте Pull Request в `dev`:

   ```bash
   git push origin feature/your-feature-name
   ```

4. После ревью и мерджа в `dev`, изменения автоматически деплоятся в dev окружение

5. Для деплоя в production создайте Pull Request из `dev` в `main`

## 🛠 Технологии

- **Next.js 14** - React фреймворк
- **TypeScript** - Типизация
- **ESLint** - Линтинг кода
- **Prettier** - Форматирование кода
- **GitHub Actions** - CI/CD

## 📦 Структура проекта

```
.
├── app/                 # Next.js App Router
│   ├── layout.tsx      # Корневой layout
│   ├── page.tsx        # Главная страница
│   └── globals.css     # Глобальные стили
├── .github/
│   └── workflows/      # GitHub Actions workflows
│       ├── ci.yml      # CI pipeline
│       ├── deploy.yml  # Production деплой
│       └── dev-deploy.yml # Dev деплой
├── next.config.js      # Конфигурация Next.js
├── tsconfig.json       # Конфигурация TypeScript
├── .eslintrc.json      # Конфигурация ESLint
├── .prettierrc         # Конфигурация Prettier
└── package.json        # Зависимости проекта
```

## 🔧 Настройка деплоя

Для настройки автоматического деплоя:

1. Откройте `.github/workflows/deploy.yml` и `.github/workflows/dev-deploy.yml`
2. Добавьте команды деплоя в соответствующие шаги
3. Настройте секреты в GitHub Settings → Secrets and variables → Actions

Примеры провайдеров:

- **Vercel**: `npx vercel --prod --token ${{ secrets.VERCEL_TOKEN }}`
- **AWS**: Используйте AWS CLI или Serverless Framework
- **Docker**: Соберите образ и запушьте в registry

## 📚 Дополнительные ресурсы

- [Next.js Documentation](https://nextjs.org/docs)
- [TypeScript Documentation](https://www.typescriptlang.org/docs)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
